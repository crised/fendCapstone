import {setElements} from '../src/client/js/app'

describe("Testing the setElements functionality", () => {
    test("Testing the setElements() function", () => {
        expect(setElements).toBeDefined();
    })
});